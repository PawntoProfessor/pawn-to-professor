'use client'
import {useState} from 'react'
import {createClient} from '@/lib/supabase/client'
export default function LoginPage(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[msg,setMsg]=useState('');async function signIn(e:React.FormEvent){e.preventDefault();setMsg('');try{const supabase=createClient();const{error}=await supabase.auth.signInWithPassword({email,password});if(error){setMsg(error.message);return}window.location.href='/admin'}catch{setMsg('Database is not connected yet. Follow SETUP-GUIDE.md first.')}}return <main className="wrap"><div className="panel authbox"><div className="login-icon">♟️</div><h1>Website Admin</h1><p>Log in to manage Pawn to Professor.</p><form onSubmit={signIn}><label className="field">Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label className="field">Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label>{msg&&<p className="notice">{msg}</p>}<button className="btn" type="submit">Log in</button></form></div></main>}
